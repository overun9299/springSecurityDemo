server:
  port: 9304
  servlet:
    context-path: /security


spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/spring_security?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true
    username: root
    password: root


  redis:
    database: 0
    host: localhost
    port: 6379

mybatis:
  mapper-locations: classpath:mapper/*.xml
